Drop static project screenshots here (e.g. `the-ai-ecom-01.png`, `nova-buds-01.png`)
if you'd rather use images than the live iframe previews used by default in
`sections/ProjectCard.tsx` and `sections/ProjectModal.tsx`.
